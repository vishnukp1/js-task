
// i have a text(no space) in the text i need to check another text include how to do with js

const mainText = "ThisIsATestText";
const searchText = "Test";

if (mainText.includes(searchText)) {
  console.log("The main text contains the search text.");
} else {
  console.log("The main text does not contain the search text.");
}
